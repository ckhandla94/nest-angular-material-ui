import { Module } from '@nestjs/common';

@Module({
  imports: [
    // ConfigModule.forRoot({
    //   load: [configuration],
    // }),
  ],
  controllers: [],
  providers: [

  ],
})
export class SharedModule { }

